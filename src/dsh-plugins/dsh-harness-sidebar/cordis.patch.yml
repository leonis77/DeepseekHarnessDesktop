# dsh-harness-sidebar bundle 层：
# 在侧栏底部注册「会话管理」入口，点击跳转到 Harness UI 的会话管理页（/__manage）。
# 注：曾尝试禁用 client-ui-workspace 隐藏原生列表，会导致 dsh UI 空白，已回滚。
- insert:
    - id: harness-sidebar
      name: dsh-harness-sidebar
